# 自检技能 · Level 3 资源

本文件用于验证 Skills 的 Level 3（附加资源）加载链路。

    L3-TOKEN = TYER-REF-2B8E41

如果智能体的回答里出现了上面这个值，说明它确实通过 `read_skill` 读取了本文件，
而不是仅凭 SKILL.md 正文作答。

这个口令是随机生成的、无语义的字符串，模型不可能猜中——
这正是它能作为「Level 3 是否真的打通」判据的原因。
